// import { UserModel} from './employeepersonaldetails';

import { UserModel } from "./usermodel";

describe('taskDetails', () => {
  it('should create an instance', () => {
    expect(new UserModel()).toBeTruthy();
  });
});